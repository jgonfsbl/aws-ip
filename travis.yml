sudo: required
dist: bionic
language: python
python:
  - "3.7"
before_install:
  - python --version
  - uname -a
  - lsb_release -a
install:
  - pip install pylint
  - pip install -r requirements.txt
script:
  - pylint --version
  - pylint app.py
